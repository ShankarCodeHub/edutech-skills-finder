export default function SQLDetails() {
  return (
    <section className="p-6">
      <h1 className="text-2xl font-bold mb-4 text-blue-700">SQL Learning Path</h1>
      <p>Learn how to query and manipulate databases using SQL.</p>
      <ul className="list-disc list-inside mt-2">
        <li>SELECT, WHERE, ORDER BY clauses</li>
        <li>Joins: INNER JOIN, LEFT JOIN, RIGHT JOIN</li>
        <li>Aggregate functions and GROUP BY</li>
      </ul>
    </section>
  );
}
